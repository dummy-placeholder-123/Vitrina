name: Deploy Vitrina

on:
  push:
    branches: [ main ]
    paths:
      - 'vitrina-mp/**'
      - '.github/**'
  workflow_dispatch:
    inputs:
      target:
        description: "Deploy target"
        type: choice
        options:
          - stage
          - prod
        default: stage
permissions:
  id-token: write
  contents: read

jobs:
  dev:
    name: Deploy Dev
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'push' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ vars.AWS_ACCOUNT_ID }}:role/custom-gh-cicd-role
          aws-region: ${{ vars.AWS_REGION }}

      - name: Validate required variables
        run: |
          set -euo pipefail
          : "${ECS_CLUSTER:?Missing vars.ECS_CLUSTER}"
          : "${ECS_SERVICE_VITRINA_MP:?Missing vars.ECS_SERVICE_VITRINA_MP}"
          : "${ECR_REPOSITORY:?Missing vars.ECR_REPOSITORY}"
        env:
          ECS_CLUSTER: ${{ vars.ECS_CLUSTER }}
          ECS_SERVICE_VITRINA_MP: ${{ vars.ECS_SERVICE_VITRINA_MP }}
          ECR_REPOSITORY: ${{ vars.ECR_REPOSITORY }}

      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push image
        run: |
          REGISTRY='${{ steps.login-ecr.outputs.registry }}'
          REPOSITORY='${{ vars.ECR_REPOSITORY }}'
          TAG='${{ github.sha }}'
          IMAGE="$REGISTRY/$REPOSITORY:$TAG"

          docker build -t "$IMAGE" -f vitrina-mp/Dockerfile vitrina-mp
          docker push "$IMAGE"

          DIGEST="$(aws ecr describe-images \
            --repository-name "$REPOSITORY" \
            --image-ids imageTag="$TAG" \
            --query 'imageDetails[0].imageDigest' \
            --output text)"
          echo "IMAGE_URI=$REGISTRY/$REPOSITORY@$DIGEST" >> "$GITHUB_ENV"

      - name: Render ECS task definition (image)
        id: render-task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: .github/workflows/ecs-task-def.json
          container-name: alpha-vitrina-mp
          image: ${{ env.IMAGE_URI }}

      - name: Deploy to ECS (dev)
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: ${{ steps.render-task-def.outputs.task-definition }}
          cluster: ${{ vars.ECS_CLUSTER }}
          service: ${{ vars.ECS_SERVICE_VITRINA_MP }}
          wait-for-service-stability: true

  stage:
    name: Deploy Stage
    runs-on: ubuntu-latest
    environment: stage
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.target == 'stage' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ vars.AWS_ACCOUNT_ID }}:role/custom-gh-cicd-role
          aws-region: ${{ vars.AWS_REGION }}

      - name: Validate required variables
        run: |
          set -euo pipefail
          : "${ECS_CLUSTER:?Missing vars.ECS_CLUSTER}"
          : "${ECS_SERVICE_VITRINA_MP:?Missing vars.ECS_SERVICE_VITRINA_MP}"
          : "${ECR_REPOSITORY:?Missing vars.ECR_REPOSITORY}"
        env:
          ECS_CLUSTER: ${{ vars.ECS_CLUSTER }}
          ECS_SERVICE_VITRINA_MP: ${{ vars.ECS_SERVICE_VITRINA_MP }}
          ECR_REPOSITORY: ${{ vars.ECR_REPOSITORY }}

      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Tag image as staged-tag
        run: |
          REGISTRY='${{ steps.login-ecr.outputs.registry }}'
          REPOSITORY='${{ vars.ECR_REPOSITORY }}'
          SOURCE_TAG='${{ github.sha }}'
          STAGED_TAG='staged-tag'

          MANIFEST="$(aws ecr batch-get-image \
            --repository-name "$REPOSITORY" \
            --image-ids imageTag="$SOURCE_TAG" \
            --query 'images[0].imageManifest' \
            --output text)"
          aws ecr put-image \
            --repository-name "$REPOSITORY" \
            --image-tag "$STAGED_TAG" \
            --image-manifest "$MANIFEST" >/dev/null

          DIGEST="$(aws ecr describe-images \
            --repository-name "$REPOSITORY" \
            --image-ids imageTag="$STAGED_TAG" \
            --query 'imageDetails[0].imageDigest' \
            --output text)"
          echo "IMAGE_URI=$REGISTRY/$REPOSITORY@$DIGEST" >> "$GITHUB_ENV"

      - name: Render ECS task definition (image)
        id: render-task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: .github/workflows/ecs-task-def.json
          container-name: alpha-vitrina-mp
          image: ${{ env.IMAGE_URI }}

      - name: Deploy to ECS (stage)
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: ${{ steps.render-task-def.outputs.task-definition }}
          cluster: ${{ vars.ECS_CLUSTER }}
          service: ${{ vars.ECS_SERVICE_VITRINA_MP }}
          wait-for-service-stability: true

  prod:
    name: Deploy Prod
    runs-on: ubuntu-latest
    environment: prod
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.target == 'prod' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ vars.AWS_ACCOUNT_ID }}:role/custom-gh-cicd-role
          aws-region: ${{ vars.AWS_REGION }}

      - name: Validate required variables
        run: |
          set -euo pipefail
          : "${ECS_CLUSTER:?Missing vars.ECS_CLUSTER}"
          : "${ECS_SERVICE_VITRINA_MP:?Missing vars.ECS_SERVICE_VITRINA_MP}"
          : "${ECR_REPOSITORY:?Missing vars.ECR_REPOSITORY}"
        env:
          ECS_CLUSTER: ${{ vars.ECS_CLUSTER }}
          ECS_SERVICE_VITRINA_MP: ${{ vars.ECS_SERVICE_VITRINA_MP }}
          ECR_REPOSITORY: ${{ vars.ECR_REPOSITORY }}

      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Use staged-tag image
        run: |
          REGISTRY='${{ steps.login-ecr.outputs.registry }}'
          REPOSITORY='${{ vars.ECR_REPOSITORY }}'
          STAGED_TAG='staged-tag'

          DIGEST="$(aws ecr describe-images \
            --repository-name "$REPOSITORY" \
            --image-ids imageTag="$STAGED_TAG" \
            --query 'imageDetails[0].imageDigest' \
            --output text)"
          echo "IMAGE_URI=$REGISTRY/$REPOSITORY@$DIGEST" >> "$GITHUB_ENV"

      - name: Render ECS task definition (image)
        id: render-task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: .github/workflows/ecs-task-def.json
          container-name: alpha-vitrina-mp
          image: ${{ env.IMAGE_URI }}

      - name: Deploy to ECS (prod)
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: ${{ steps.render-task-def.outputs.task-definition }}
          cluster: ${{ vars.ECS_CLUSTER }}
          service: ${{ vars.ECS_SERVICE_VITRINA_MP }}
          wait-for-service-stability: true
